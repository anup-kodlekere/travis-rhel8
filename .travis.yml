jobs:
  include:
    - os: linux
      dist: rhel8
      language: c
      compiler:
        - gcc
      script:
        - echo "Testing C Compiler (gcc)"
        - gcc --version
        - gcc r8.c
        - ./a.out
